# Ultralight Code Assistant — Dependencies

# Core inference (GGUF model loading)
llama-cpp-python>=0.2.0

# Embeddings + semantic search
faiss-cpu>=1.7.4
sentence-transformers>=2.2.0
numpy>=1.24.0

# Configuration
pyyaml>=6.0

# Terminal UI
rich>=13.0.0

# REST API + web UI
fastapi>=0.100.0
uvicorn>=0.20.0

# Smart routing (neural classifier)
scikit-learn>=1.3.0

# Model download
huggingface-hub>=0.20.0

# Desktop app (optional — falls back to browser if not installed)
pywebview>=4.0

# Code intelligence (optional — only needed for the --lsp tool set)
# Pure-Python, in-process. Remove this line + delete engine/agent_lsp.py to fully unwire.
jedi>=0.19.0
